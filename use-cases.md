# Catchmind Use Cases (유스케이스)

## 목차
1. [사용자 관리](#1-사용자-관리)
2. [방 관리](#2-방-관리)
3. [게임 진행](#3-게임-진행)
4. [그림 그리기](#4-그림-그리기)
5. [정답 맞추기](#5-정답-맞추기)
6. [채팅](#6-채팅)
7. [게임 종료](#7-게임-종료)

---

## 1. 사용자 관리

### UC-1.1: 게스트로 참여하기

**액터**: 방문자

**사전 조건**: 없음

**주 시나리오**:
1. 방문자가 닉네임을 입력한다
2. 시스템은 닉네임을 검증한다 (1-20자)
3. 시스템은 게스트 유저를 생성한다
4. 시스템은 JWT 토큰을 발급한다
5. 시스템은 유저 정보와 토큰을 반환한다

**대체 시나리오**:
- 2a. 닉네임이 유효하지 않음
  - 2a1. 시스템은 에러 메시지를 표시한다
  - 2a2. 유스케이스를 종료한다

**사후 조건**:
- 게스트 유저가 생성되고 인증 토큰을 받는다
- 로비 화면으로 이동한다

---

## 2. 방 관리

### UC-2.1: 방 생성하기

**액터**: 인증된 유저

**사전 조건**: 유저가 로그인되어 있음

**주 시나리오**:
1. 유저가 "방 만들기"를 선택한다
2. 유저가 방 제목, 최대 인원, 카테고리, 난이도를 입력한다
3. 시스템은 입력값을 검증한다
4. 시스템은 8자리 고유 방 코드를 생성한다
5. 시스템은 방을 생성하고 유저를 방장으로 설정한다
6. 시스템은 유저를 방에 자동 참가시킨다
7. 시스템은 방 정보를 반환한다
8. 유저는 게임 대기 화면으로 이동한다

**대체 시나리오**:
- 3a. 입력값이 유효하지 않음
  - 3a1. 시스템은 에러 메시지를 표시한다
  - 3a2. 2단계로 돌아간다
- 4a. 방 코드 생성 실패 (중복)
  - 4a1. 시스템은 다시 방 코드를 생성한다

**사후 조건**:
- 새로운 방이 생성되고 유저가 방장으로 참가한다
- 방 상태는 'waiting'이다

---

### UC-2.2: 방 참가하기

**액터**: 인증된 유저

**사전 조건**: 유저가 로그인되어 있음

**주 시나리오**:
1. 유저가 방 코드를 입력한다
2. 시스템은 방 코드로 방을 검색한다
3. 시스템은 방의 참가 가능 여부를 확인한다
   - 방이 존재하는지
   - 방 상태가 'waiting'인지
   - 최대 인원을 초과하지 않는지
   - 이미 참가한 유저인지
4. 시스템은 유저를 방에 참가시킨다
5. 시스템은 방의 모든 플레이어에게 `PlayerJoined` 이벤트를 전송한다
6. 시스템은 방 정보를 반환한다
7. 유저는 게임 대기 화면으로 이동한다

**대체 시나리오**:
- 2a. 방을 찾을 수 없음
  - 2a1. 시스템은 "존재하지 않는 방 코드입니다" 메시지를 표시한다
  - 2a2. 유스케이스를 종료한다
- 3a. 방이 이미 게임 중
  - 3a1. 시스템은 "게임이 진행 중인 방입니다" 메시지를 표시한다
  - 3a2. 유스케이스를 종료한다
- 3b. 방이 가득 참
  - 3b1. 시스템은 "방이 가득 찼습니다" 메시지를 표시한다
  - 3b2. 유스케이스를 종료한다
- 3c. 이미 참가한 유저
  - 3c1. 시스템은 "이미 참가한 방입니다" 메시지를 표시한다
  - 3c2. 유스케이스를 종료한다

**사후 조건**:
- 유저가 방에 참가한다
- 다른 플레이어들이 새 참가자를 확인할 수 있다

---

### UC-2.3: 방 설정 변경하기

**액터**: 방장

**사전 조건**:
- 유저가 방에 참가해 있음
- 유저가 방장임
- 방 상태가 'waiting'임

**주 시나리오**:
1. 방장이 설정 메뉴를 연다
2. 방장이 최대 인원, 카테고리, 난이도를 변경한다
3. 시스템은 변경값을 검증한다
4. 시스템은 방 설정을 업데이트한다
5. 시스템은 방의 모든 플레이어에게 `SettingsUpdated` 이벤트를 전송한다

**대체 시나리오**:
- 3a. 변경값이 유효하지 않음
  - 3a1. 시스템은 에러 메시지를 표시한다
  - 3a2. 2단계로 돌아간다

**사후 조건**:
- 방 설정이 업데이트된다
- 모든 플레이어가 변경된 설정을 확인할 수 있다

---

### UC-2.4: 방 나가기

**액터**: 방 참가자

**사전 조건**: 유저가 방에 참가해 있음

**주 시나리오**:
1. 유저가 "나가기" 버튼을 클릭한다
2. 시스템은 유저를 방에서 제거한다
3. 시스템은 방의 남은 플레이어에게 `PlayerLeft` 이벤트를 전송한다
4. **IF** 유저가 방장이었다면:
   - 4a. 시스템은 다음 참가자를 새 방장으로 지정한다
   - 4b. 시스템은 `HostChanged` 이벤트를 전송한다
5. **IF** 방에 남은 플레이어가 없다면:
   - 5a. 시스템은 방을 삭제한다
6. 유저는 로비 화면으로 이동한다

**대체 시나리오**:
- 4a. 게임 진행 중 방장이 나감
  - 4a1. 시스템은 현재 라운드를 종료한다
  - 4a2. 4단계로 진행한다

**사후 조건**:
- 유저가 방에서 제거된다
- 필요 시 새 방장이 지정된다
- 빈 방은 삭제된다

---

## 3. 게임 진행

### UC-3.1: 게임 시작하기

**액터**: 방장

**사전 조건**:
- 유저가 방장임
- 방에 최소 2명 이상 참가해 있음
- 방 상태가 'waiting'임

**주 시나리오**:
1. 방장이 "게임 시작" 버튼을 클릭한다
2. 시스템은 참가 인원을 확인한다
3. 시스템은 총 라운드 수를 계산한다
   - 2-3명: `인원 × 2`
   - 4-6명: `인원 × 1`
4. 시스템은 Drawer 순서를 결정한다 (랜덤 또는 순차)
5. 시스템은 게임 세션을 생성한다
6. 시스템은 방 상태를 'playing'으로 변경한다
7. 시스템은 모든 플레이어의 점수를 0으로 초기화한다
8. 시스템은 모든 플레이어에게 `GameStarted` 이벤트를 전송한다
9. 시스템은 첫 번째 라운드를 시작한다 (UC-3.2)

**대체 시나리오**:
- 2a. 참가 인원이 2명 미만
  - 2a1. 시스템은 "최소 2명 이상 필요합니다" 메시지를 표시한다
  - 2a2. 유스케이스를 종료한다

**사후 조건**:
- 게임이 시작되고 첫 라운드가 진행된다
- 방 상태가 'playing'이다

---

### UC-3.2: 라운드 시작하기

**액터**: 시스템

**사전 조건**:
- 게임이 진행 중임
- 현재 라운드가 총 라운드 수 미만임

**주 시나리오**:
1. 시스템은 현재 차례의 Drawer를 결정한다
2. 시스템은 라운드 데이터를 생성한다
3. 시스템은 게임 phase를 'choosing'으로 변경한다
4. 시스템은 카테고리와 난이도에 맞는 제시어 4개를 선택한다
5. 시스템은 Drawer에게만 `round/wordChoices` 이벤트를 전송한다
6. 시스템은 다른 플레이어에게 "OOO님이 단어를 선택 중입니다" 메시지를 표시한다
7. 시스템은 제시어 선택 타이머를 시작한다 (30초)

**대체 시나리오**:
- 7a. 30초 내에 단어를 선택하지 않음
  - 7a1. 시스템은 랜덤으로 단어를 선택한다
  - 7a2. UC-3.3으로 진행한다

**사후 조건**:
- 라운드가 시작되고 Drawer에게 제시어 선택지가 제공된다

---

### UC-3.3: 제시어 선택하기

**액터**: Drawer

**사전 조건**:
- 라운드가 'choosing' 상태임
- 유저가 현재 Drawer임

**주 시나리오**:
1. Drawer가 4개 선택지 중 하나를 선택한다
2. 시스템은 선택된 제시어를 라운드에 저장한다
3. 시스템은 힌트를 생성한다 (글자 수 기반)
4. 시스템은 게임 phase를 'drawing'으로 변경한다
5. 시스템은 60초 타이머를 시작한다
6. 시스템은 모든 플레이어에게 `round/start` 이벤트를 전송한다
   - Drawer: 실제 제시어 표시
   - Guesser: 힌트 표시 (예: "3글자")
7. 시스템은 봇 메시지를 전송한다: "보미쌤: OOO님, 그림을 그려주세요"

**사후 조건**:
- 제시어가 선택되고 그리기 단계가 시작된다
- Drawer는 그림을 그릴 수 있다
- Guesser는 답을 맞출 수 있다

---

### UC-3.4: 라운드 종료하기

**액터**: 시스템

**사전 조건**:
- 라운드가 'drawing' 상태임
- 종료 조건 충족:
  - 타이머가 0초에 도달, 또는
  - 모든 Guesser가 정답을 맞춤

**주 시나리오**:
1. 시스템은 라운드를 종료한다
2. 시스템은 Drawer의 점수를 계산한다
   - 최소 1명 정답: +5점
   - 모두 정답: 추가 +3점
3. 시스템은 게임 phase를 'roundEnd'로 변경한다
4. 시스템은 모든 플레이어에게 `round/end` 이벤트를 전송한다
   - 정답 공개
   - 라운드 점수 요약
5. 시스템은 봇 메시지를 전송한다: "보미쌤: 이번 라운드가 종료되었어요! 정답은: OOO"
6. 시스템은 3초 대기한다
7. **IF** 다음 라운드가 존재한다면:
   - 7a. UC-3.2로 진행한다 (다음 라운드 시작)
8. **ELSE**:
   - 8a. UC-3.5로 진행한다 (게임 종료)

**사후 조건**:
- 라운드가 종료되고 점수가 집계된다
- 다음 라운드 또는 게임 종료로 진행된다

---

### UC-3.5: 게임 종료하기

**액터**: 시스템

**사전 조건**:
- 모든 라운드가 완료됨

**주 시나리오**:
1. 시스템은 최종 순위를 계산한다
   - 누적 점수 기준 내림차순
   - 동점 시: 1등 횟수 → userId 오름차순
2. 시스템은 게임 phase를 'gameEnd'로 변경한다
3. 시스템은 방 상태를 'finished'로 변경한다
4. 시스템은 모든 플레이어에게 `game/end` 이벤트를 전송한다
5. 시스템은 최종 순위 모달을 표시한다
   - 1위, 2위, 3위 표시
   - 각 플레이어의 점수
6. 시스템은 "나가기", "한판 더하기" 버튼을 표시한다

**사후 조건**:
- 게임이 종료되고 최종 순위가 표시된다
- 플레이어는 나가거나 재시작할 수 있다

---

### UC-3.6: 한판 더하기

**액터**: 방장

**사전 조건**:
- 게임이 종료됨 (phase = 'gameEnd')
- 유저가 방장임

**주 시나리오**:
1. 방장이 "한판 더하기" 버튼을 클릭한다
2. 시스템은 게임 종료 모달을 닫는다
3. 시스템은 방 상태를 'waiting'으로 변경한다
4. 시스템은 모든 플레이어의 점수를 0으로 리셋한다
5. 시스템은 이전 게임 데이터를 저장한다 (선택)
6. 방장이 준비되면 UC-3.1을 실행한다 (게임 시작)

**사후 조건**:
- 게임이 리셋되고 대기 상태로 돌아간다
- 동일한 플레이어로 새 게임을 시작할 수 있다

---

## 4. 그림 그리기

### UC-4.1: 캔버스에 그리기

**액터**: Drawer

**사전 조건**:
- 라운드가 'drawing' 상태임
- 유저가 현재 Drawer임

**주 시나리오**:
1. Drawer가 캔버스에 마우스/터치로 그림을 그린다
2. 클라이언트는 스트로크 데이터를 수집한다
   - 좌표 배열 `[{x, y}]`
   - 색상, 선 두께, 도구 타입
3. 클라이언트는 스트로크를 throttling 처리한다 (너무 빈번한 전송 방지)
4. 클라이언트는 서버에 `draw/stroke` 이벤트를 전송한다
5. 서버는 Drawer 권한을 확인한다
6. 서버는 모든 플레이어에게 `draw/stroke` 이벤트를 브로드캐스트한다
7. 각 클라이언트는 캔버스를 업데이트한다

**대체 시나리오**:
- 5a. 유저가 Drawer가 아님
  - 5a1. 서버는 요청을 무시한다
  - 5a2. 유스케이스를 종료한다

**사후 조건**:
- 모든 플레이어의 캔버스가 실시간으로 동기화된다

---

### UC-4.2: 캔버스 지우기

**액터**: Drawer

**사전 조건**:
- 라운드가 'drawing' 상태임
- 유저가 현재 Drawer임

**주 시나리오**:
1. Drawer가 "전체 지우기" 버튼을 클릭한다
2. 클라이언트는 로컬 캔버스를 지운다
3. 클라이언트는 서버에 `draw/clear` 이벤트를 전송한다
4. 서버는 Drawer 권한을 확인한다
5. 서버는 모든 플레이어에게 `draw/clear` 이벤트를 브로드캐스트한다
6. 각 클라이언트는 캔버스를 지운다

**사후 조건**:
- 모든 플레이어의 캔버스가 지워진다

---

### UC-4.3: 그리기 도구 변경

**액터**: Drawer

**사전 조건**:
- 라운드가 'drawing' 상태임
- 유저가 현재 Drawer임

**주 시나리오**:
1. Drawer가 그리기 도구를 선택한다
   - 펜 도구
   - 색상 (팔레트에서 선택)
   - 선 두께 (1-20)
   - 지우개
2. 클라이언트는 로컬 도구 설정을 업데이트한다
3. 이후 그리기는 변경된 설정을 사용한다

**사후 조건**:
- Drawer의 그리기 도구가 변경된다

---

## 5. 정답 맞추기

### UC-5.1: 정답 제출하기

**액터**: Guesser

**사전 조건**:
- 라운드가 'drawing' 상태임
- 유저가 Guesser임 (Drawer 아님)
- 유저가 아직 정답을 맞추지 않음

**주 시나리오**:
1. Guesser가 채팅창에 답을 입력한다
2. 클라이언트는 서버에 `chat/message` 이벤트를 전송한다
3. 서버는 답을 normalize한다
   - trim() - 앞뒤 공백 제거
   - replace(/\s+/g, '') - 중간 공백 제거
   - toLowerCase() - 소문자 변환 (영문)
4. 서버는 정답과 비교한다
5. **IF** 정답이 맞다면:
   - 5a. UC-5.2로 진행한다 (정답 처리)
6. **ELSE**:
   - 6a. UC-6.1로 진행한다 (일반 채팅 처리)

**사후 조건**:
- 답이 정답인지 판정된다

---

### UC-5.2: 정답 처리하기

**액터**: 시스템

**사전 조건**:
- 유저의 답이 정답과 일치함
- 유저가 아직 정답을 맞추지 않음

**주 시나리오**:
1. 시스템은 정답 순서를 기록한다
2. 시스템은 점수를 계산한다
   - 기본 점수 (순서별): 1등=10, 2등=7, 3등=5, 4등 이후=3
   - 시간 보너스: `Math.floor(remainingSeconds / 6)`
   - 최종 점수: `baseScore + timeBonus`
3. 시스템은 유저의 누적 점수를 업데이트한다
4. 시스템은 유저 상태를 'answered'로 변경한다
5. 시스템은 RoundAnswer를 생성한다
6. 시스템은 모든 플레이어에게 `round/answered` 이벤트를 전송한다
   - 정답 맞춘 유저 정보
   - 획득 점수
   - 누적 점수
7. 시스템은 봇 메시지를 전송한다: "보미쌤: OOO님이 정답을 맞추셨어요!"
8. 시스템은 정답 맞춘 유저의 채팅을 "정답!"으로 표시한다 (다른 유저에게)
9. **IF** 모든 Guesser가 정답을 맞췄다면:
   - 9a. UC-3.4로 진행한다 (라운드 종료)

**대체 시나리오**:
- Pre-1. 유저가 이미 정답을 맞춤
  - Pre-1a. 시스템은 "이미 정답을 맞추셨습니다" 메시지를 표시한다
  - Pre-1b. 채팅 메시지로만 처리한다
  - Pre-1c. 유스케이스를 종료한다

**사후 조건**:
- 유저의 점수가 업데이트된다
- 모든 플레이어가 정답 여부를 확인할 수 있다

---

## 6. 채팅

### UC-6.1: 채팅 메시지 전송

**액터**: 방 참가자

**사전 조건**: 유저가 방에 참가해 있음

**주 시나리오**:
1. 유저가 채팅창에 메시지를 입력한다
2. 클라이언트는 서버에 `chat/message` 이벤트를 전송한다
3. **IF** 라운드가 'drawing' 상태이고 유저가 Guesser라면:
   - 3a. UC-5.1로 진행한다 (정답 확인)
4. 서버는 욕설 필터링을 수행한다
5. 시스템은 ChatMessage를 생성한다
6. 시스템은 모든 플레이어에게 `chat/broadcast` 이벤트를 전송한다
7. 각 클라이언트는 채팅 로그에 메시지를 추가한다
8. 시스템은 유저의 lastChat을 업데이트한다 (유저 썸네일에 표시)

**대체 시나리오**:
- 4a. 욕설이 감지됨
  - 4a1. 시스템은 욕설을 필터링한다 (예: "***")
  - 4a2. 6단계로 진행한다

**사후 조건**:
- 채팅 메시지가 모든 플레이어에게 표시된다

---

### UC-6.2: 시스템 메시지 전송

**액터**: 시스템

**사전 조건**: 특정 게임 이벤트 발생

**주 시나리오**:
1. 시스템은 게임 이벤트에 따라 메시지를 생성한다
   - 라운드 시작: "보미쌤: OOO님, 그림을 그려주세요"
   - 힌트: "보미쌤: 힌트: 3글자"
   - 정답: "보미쌤: OOO님이 정답을 맞추셨어요!"
   - 라운드 종료: "보미쌤: 이번 라운드가 종료되었어요! 정답은: OOO"
2. 시스템은 ChatMessage를 생성한다 (type='system', userId=null)
3. 시스템은 모든 플레이어에게 `chat/broadcast` 이벤트를 전송한다
4. 각 클라이언트는 시스템 메시지를 구분하여 표시한다 (봇 아이콘)

**사후 조건**:
- 시스템 메시지가 모든 플레이어에게 표시된다

---

## 7. 게임 종료

### UC-7.1: 게임 중 나가기

**액터**: 방 참가자

**사전 조건**:
- 유저가 방에 참가해 있음
- 게임이 진행 중임

**주 시나리오**:
1. 유저가 "나가기" 버튼을 클릭한다
2. 시스템은 확인 다이얼로그를 표시한다
3. 유저가 확인을 선택한다
4. **IF** 유저가 현재 Drawer라면:
   - 4a. 시스템은 현재 라운드를 즉시 종료한다 (UC-3.4)
5. 시스템은 유저를 방에서 제거한다 (UC-2.4)
6. 시스템은 봇 메시지를 전송한다: "보미쌤: OOO님이 나가셨습니다"
7. **IF** 남은 플레이어가 1명 미만이라면:
   - 7a. 시스템은 게임을 종료한다 (UC-3.5)

**대체 시나리오**:
- 3a. 유저가 취소를 선택
  - 3a1. 유스케이스를 종료한다

**사후 조건**:
- 유저가 게임에서 나간다
- 필요 시 게임이 종료된다

---

### UC-7.2: 연결 끊김 처리

**액터**: 시스템

**사전 조건**: 유저의 연결이 끊김

**주 시나리오**:
1. 시스템은 유저의 연결 끊김을 감지한다
2. 시스템은 30초 동안 재접속을 기다린다
3. **IF** 30초 내에 재접속하면:
   - 3a. UC-7.3으로 진행한다 (재접속 처리)
4. **ELSE**:
   - 4a. UC-7.1로 진행한다 (강제 퇴장)

**사후 조건**:
- 유저가 재접속하거나 강제 퇴장된다

---

### UC-7.3: 재접속하기

**액터**: 방 참가자

**사전 조건**:
- 유저가 게임 중 연결이 끊김
- 30초 이내에 재접속

**주 시나리오**:
1. 유저가 재접속한다
2. 클라이언트는 인증 토큰과 함께 서버에 연결한다
3. 서버는 유저의 방 정보를 조회한다
4. 서버는 현재 방 상태를 전송한다 (RoomState)
   - 모든 플레이어 정보
   - 현재 게임 상태
   - 현재 라운드 정보
   - 캔버스 데이터 (현재 라운드의 모든 스트로크)
   - 최근 채팅 메시지 (최근 50개)
5. 클라이언트는 게임 화면을 복구한다
6. 시스템은 봇 메시지를 전송한다: "보미쌤: OOO님이 다시 돌아오셨습니다"

**사후 조건**:
- 유저가 현재 게임 상태로 복구된다
- 게임이 계속 진행된다

---

## 8. 타이머 관리

### UC-8.1: 라운드 타이머 진행

**액터**: 시스템

**사전 조건**:
- 라운드가 'drawing' 상태임

**주 시나리오**:
1. 시스템은 60초 타이머를 시작한다
2. 매 1초마다:
   - 2a. 시스템은 remainingSeconds를 1 감소시킨다
   - 2b. 시스템은 모든 플레이어에게 `timer/update` 이벤트를 전송한다
3. **IF** remainingSeconds가 30초일 때:
   - 3a. 시스템은 추가 힌트를 제공한다 (선택 기능)
4. **IF** remainingSeconds가 0에 도달하면:
   - 4a. 시스템은 `timer/expired` 이벤트를 전송한다
   - 4b. UC-3.4로 진행한다 (라운드 종료)

**사후 조건**:
- 타이머가 0에 도달하면 라운드가 종료된다

---

## 9. 액터 정의

### 9.1 주요 액터

**방문자 (Visitor)**
- 아직 인증되지 않은 사용자
- 게스트로 참여 가능

**인증된 유저 (Authenticated User)**
- 게스트 계정으로 참여한 사용자
- 방 생성 및 참가 가능

**방 참가자 (Room Participant)**
- 특정 방에 참가한 유저
- 채팅 및 게임 참여 가능

**방장 (Host)**
- 방을 생성한 유저 또는 권한을 이전받은 유저
- 방 설정 변경 및 게임 시작 권한 보유

**Drawer**
- 현재 라운드에서 그림을 그리는 플레이어
- 캔버스 그리기 권한 보유
- 정답 입력 불가

**Guesser**
- 현재 라운드에서 그림을 맞추는 플레이어
- 채팅으로 정답 제출 가능
- 캔버스 읽기 전용

### 9.2 시스템 액터

**시스템 (System)**
- 자동화된 게임 진행 로직
- 타이머 관리
- 이벤트 브로드캐스트

**봇 (Bot / 보미쌤)**
- 시스템 메시지를 전달하는 가상의 관리자
- 게임 흐름 안내

---

## 10. 유스케이스 다이어그램 (텍스트)

```
[방문자]
  ├─ UC-1.1: 게스트로 참여하기

[인증된 유저]
  ├─ UC-2.1: 방 생성하기
  ├─ UC-2.2: 방 참가하기
  └─ UC-2.4: 방 나가기

[방장]
  ├─ UC-2.3: 방 설정 변경하기
  ├─ UC-3.1: 게임 시작하기
  └─ UC-3.6: 한판 더하기

[Drawer]
  ├─ UC-4.1: 캔버스에 그리기
  ├─ UC-4.2: 캔버스 지우기
  ├─ UC-4.3: 그리기 도구 변경
  └─ UC-3.3: 제시어 선택하기

[Guesser]
  └─ UC-5.1: 정답 제출하기

[방 참가자]
  ├─ UC-6.1: 채팅 메시지 전송
  ├─ UC-7.1: 게임 중 나가기
  └─ UC-7.3: 재접속하기

[시스템]
  ├─ UC-3.2: 라운드 시작하기
  ├─ UC-3.4: 라운드 종료하기
  ├─ UC-3.5: 게임 종료하기
  ├─ UC-5.2: 정답 처리하기
  ├─ UC-6.2: 시스템 메시지 전송
  ├─ UC-7.2: 연결 끊김 처리
  └─ UC-8.1: 라운드 타이머 진행
```

---

*이 문서는 기능 변경사항을 반영하여 지속적으로 업데이트됩니다.*
